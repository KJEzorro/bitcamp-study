오늘 요약

클로저 : 함수를 이용해서 상태유지 -> JAVA 클래스 - 인스턴스 처럼
(돼지저금통) ===> JS 모듈패턴 (내부에 상태를 갖게 하고 상태를 조작하는 함수를 반환


이벤트 : 버블링(가장 일반적)/캡처링 => false
	stopPropagation(전파 막는 것)/.preventDefalult(기본동작을 막는 것)

이벤트 처리시에 DOM에 없지만 작업을 통해 추가(동적으로 생성)되는 것들은 이벤트가 안걸린다.
=>> onclick

target => 실제로 이벤트가 발생한 element


구조분해할당 : const { a, b, c} = obj
전개연산자


#####
velog에 JSON에 대해서 올리기